export class NotaMidi {
  note: string
  duration: string
  time: string

  constructor(note: string, duration: string, time: string) {
    this.note = note
    this.duration = duration
    this.time = time
  }
}
